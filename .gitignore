# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.local
.env.*local

# Uploaded career resumes (do not commit)
uploads/

# Raw video masters dropped into public/ (hundreds of MB each). Re-encode for web
# before committing — see the `videos` entries in src/shared/Data.jsx.
# Exact names only: core.ignorecase is true on macOS, so a pattern like
# *Walkthrough*.mp4 would also swallow the lowercase web encodes we want tracked.
/public/Smart TruALS Adult Walkthrough with Dr McKenna.mp4
/public/Smart TruALS Adult Promo Video 1080p (1).mp4
/public/Smart TruALS Adult Promo Video 4k (1).mp4
/public/Smart TruALS Adult Promo Video 4k (1) (1).mp4
/public/ALS Set Up & Maintenance.mp4
